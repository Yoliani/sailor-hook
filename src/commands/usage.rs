//! Sync + print agent usage / rate-limit windows. Phase 0: stub. Phase 3
//! parses rate-limit windows from agent output/transcripts.

pub fn run(_sync: bool) -> anyhow::Result<()> {
    println!("usage: not yet implemented (Phase 3).");
    Ok(())
}
